+++
title = "Bristle: A Bril compiler for MinCaml"
[extra]
bio = """
  helen ge is “a remarkable programmer with delightful thoughts”, as described by pedro. she exists. you can find her as a spirit floating around ubb most days during working hours. she has no soul.

  Pedro Pontes García is “a very cool and tall and talented and humorous and kind and skilled person”, as described by Helen. He is yet to develop a human-brain compiler for the complexity of this world.
"""
[[extra.authors]]
name = "Helen Ge"
link = "https://mercure_67.codeberg.page/"
[[extra.authors]]
name = "Pedro Pontes García"
link = "https://pedropontesgarcia.com/"
+++

## What was our initial goal?

At the outset, we wanted to write a compiler from a functional language to Bril. Originally, we thought this was going to be a Lisp-like language or something of our own design, but eventually we settled upon the [MinCaml](https://esumii.github.io/min-caml/index-e.html) subset of OCaml. Time permitting, we also wanted to explore further optimising our compiled output, both at the layer of ‘defunctionalizing’ the language into the Bril IR, and on the Bril IR itself.

## What did we do?

We implemented Bristle, a Bril compiler for MinCaml. Bristle supports most of MinCaml, except for functions with free variables, arrays, or tuples. It passes all test cases from the original project for the features we support. This means passing tests for floating-point operations and some first-class functions.

We did not include the tuple or array features of MinCaml, as we felt these would be annoying and less relevant to implement in Bril. Nonetheless, arrays could be implemented relatively easily using the Bril memory extension.

We also implemented the basics of a “function reference” extension in Bril. Since it isn’t always possible to know which function will be called at compile-time, we implemented this functionality on top of the Brili interpreter and the associated TypeScript Bril library. Since we wrote our compiler in Rust, we also updated the Bril-rs library to handle our extension.

Due to the constraints of the function reference extension, which does not allow an environment to be saved, we didn’t support closures that enclose free variables. This could be developed by introducing pairs and structured types to Bril, and modifying function references to be pairs of environments as structured values and function names.

The optimizer we wrote for class, BDS, does not handle the floating-point and function reference extensions. Given that our goal was correctness and not optimization, we ended up not extending BDS to support Bristle output.

## What were the hardest parts to get right?

Most “make a programming language” projects we found in research generally focus on Lisp-like languages, and usually cheat by actually implementing an interpreter, not a compiler. Even in instances which actually “build a compiler”, the emphasis is overwhelmingly on Lisps.

We built a compiler for a typed non-Lisp language, and wanted to minimize modifications to Bril (after all, it is technically supposed to be an IR, not an interpreter). We encountered a lack of documentation around our case and skill level.

There don’t seem to be many resources which detail in depth what components are necessary for compiling a functional language, so we had to figure these out on our own. The MinCaml reference implementation was somewhat helpful, but is actually too specific—it targets machine code, which permits, for example, looser forms of handling closures and memory, which we were not interested in doing in Bril. It also proposes converting the input code into K-normal form, for which there is a lack of documentation around on the internet. Information around ML-family languages was also difficult to find due to the abundance of machine learning content.

This leads into one of the hardest tasks for us: properly supporting first-class functions within the constraints of Bril. We went down multiple rabbit holes about lambda lifting and closure conversion, only to find information that was often confusing and misleading. We found very disparate notions of what these two concepts meant. Literature around these topics was somewhat sparse, very advanced, and again often applied to different languages targeting a different set of constraints. The terminology for “lambdas”, “closures”, etc. was also used very loosely.

To make the work more manageable, we chose to focus on what outcomes we wanted to support. We wanted to support some form of closure (which we define as functions that take in state from the scope in which they are defined) and function references (assigning a function, without inputs, to a variable). We also added the function reference extension to Bril, which keeps the spirit of the language in supporting a minimal version of functionality we needed. We used the original lambda lifting implementation described in Johnsson 1984, to aid in defunctionalisation, rather than using conflicting definitions of closure conversion. It is able to lift some closures to the top level, such as in the [`cls-bug.ml` benchmark](https://codeberg.org/pedropontesgarcia/bristle/src/branch/main/test/cls-bug.ml).

Additionally, as with most compiler projects, we found parsing to be annoying. To save time, we created a MinCamlParse Dune project, to patch the reference implementation’s parser to return its output as JSON, and designed our implementation to read this input into its own parse tree in Rust. Since type inference was not part of our project, we also patched the reference type inference algorithm—an implementation of Hindley-Milner—into MinCamlParse.

## Were we successful?

We’d say so, yes! Our compiler supports a surprisingly large amount of the MinCaml specification, and we were able to run some worthwhile programs—for example, a square root approximation—using the result. While we didn’t get as much optimization in as we had hoped for, we encountered enough complexity in the core task of converting a functional language with first-class functions and lexical scope to Bril. We think that, given the unexpected challenges, accomplishing this task is sufficient.

## A list of papers we read

- Flanagan, Cormac, et al. “The Essence of Compiling with Continuations.” PLDI ’93: Proceedings of the ACM SIGPLAN 1993 Conference on Programming Language Design and Implementation, vol. 28, no. 6, June 1993, https://doi.org/10.1145/155090.155113.

- Graf, Sebastian, and Simon Peyton Jones. “Selective Lambda Lifting.” ArXiv, Cornell University, Oct. 2019, https://doi.org/10.48550/arxiv.1910.11717.

- Johnsson, Thomas. “Lambda Lifting: Transforming Programs to Recursive Equations.” Functional Programming Languages and Computer Architecture (FPCA) 1985, vol. 201, 1985, https://doi.org/10.1145/202530.202532.

- Kelsey, Richard A. “A Correspondence between Continuation Passing Style and Static Single Assignment Form.” ACM SIGPLAN Notices, vol. 30, no. 3, Mar. 1995, pp. 13–22, https://doi.org/10.1145/202530.202532. Accessed 16 Dec. 2025.

- Sumii, Eijiro. “MinCaml: A Simple and Efficient Compiler for a Minimal Functional Language.” Proceedings of the ACM 2005 Workshop on Functional and Declarative Programming in Education, Sept. 2005, pp. 27–38, https://doi.org/10.1145/1085114.1085122.